# Quarto build artifacts
# Note: _site/ IS committed for v1 (no-build deploy strategy — Netlify
# serves the pre-rendered output directly). Re-render locally via
# `quarto render` before pushing source changes.
.quarto/
_freeze/

# RStudio
.Rproj.user/
*.Rproj

# OS junk
.DS_Store
Thumbs.db

# Editor / tool caches
.vscode/
.idea/
*.log

# Node (if the netlify plugin is added later)
node_modules/

/.quarto/
**/*.quarto_ipynb
