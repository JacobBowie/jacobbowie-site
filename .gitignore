# Quarto build artifacts (Path A — Netlify builds on push via inline
# Quarto install in build.command).
_site/
.quarto/
_freeze/

# RStudio
.Rproj.user/
*.Rproj

# OS junk
.DS_Store
Thumbs.db

# Editor / tool caches
.vscode/
.idea/
*.log

# Node (no longer used — Path A doesn't need package.json or node_modules)
node_modules/

/.quarto/
**/*.quarto_ipynb

# Case-study content drafts not yet ready for the public site
_drafts/

# Local Claude session orientation files — never committed.
# Policy: CLAUDE.md and CURRENT_STATE.md are session tooling, not project
# artifacts. They were initially committed via PR #6 (2026-05-21) but
# moved to local-only on the same day. README.md is the canonical public
# orientation surface.
CLAUDE.md
CURRENT_STATE.md
_PORTFOLIO_BRIEF*.md

# Citation-network artifacts pulled at build time from
# JacobBowie/citation-network releases — not committed.
citation-network/interactive/
citation-network/notebook/
citation-network/assets/
